name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  ai-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: AI Code Review
        uses: actions/github-script@v7
        with:
          script: |
            const OPENROUTER_API_KEY = '${{ secrets.OPENROUTER_API_KEY }}';

            const diff = await github.rest.repos.compareCommits({
              owner: context.repo.owner,
              repo: context.repo.repo,
              base: context.payload.pull_request.base.sha,
              head: context.payload.pull_request.head.sha
            });

            // 리뷰할 파일 필터링
            const filesToReview = diff.data.files.filter(file =>
              file.patch && 
              !file.filename.includes('node_modules/') &&
              !file.filename.includes('.expo/') &&
              !file.filename.includes('dist/') &&
              !file.filename.endsWith('.lock')
            );

            if (filesToReview.length === 0) {
              console.log('No files to review');
              return;
            }

            // 전체 변경사항 수집
            let allChanges = '';
            const filesSummary = [];
            
            for (const file of filesToReview) {
              filesSummary.push(`- ${file.filename} (+${file.additions}/-${file.deletions})`);
              allChanges += `\n### ${file.filename}\n`;
              allChanges += `\`\`\`diff\n${file.patch}\n\`\`\`\n`;
            }

            // AI 리뷰 요청
            const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
              method: 'POST',
              headers: {
                'Authorization': `Bearer ${OPENROUTER_API_KEY}`,
                'Content-Type': 'application/json'
              },
              body: JSON.stringify({
                model: 'openrouter/sonoma-sky-alpha',
                messages: [{
                  role: 'system',
                  content: `React Native 프론트엔드 코드 리뷰어입니다.

                  ## 🔴 절대 규칙 (반드시 체크)
                  
                  1. **컴포넌트 100줄 제한** - 초과시 즉시 분리 필요
                  2. **React.memo() 필수** - 모든 리스트 아이템 컴포넌트
                  3. **스타일 위치** - 컴포넌트 내부에만 (별도 파일 금지)
                  4. **any 타입 금지** - 반드시 proper TypeScript 타입 사용
                  5. **상태 관리** - Zustand(전역), React Query(서버)
                  
                  ## 체크 포인트
                  - [ ] 100줄 넘는 컴포넌트 있는가?
                  - [ ] 리스트 아이템에 React.memo 적용했는가?
                  - [ ] any 타입 사용한 곳 있는가?
                  - [ ] StyleSheet.create() 사용했는가?
                  - [ ] 불필요한 prop drilling 있는가?`
                }, {
                  role: 'user',
                  content: `다음 ${filesToReview.length}개 파일 리뷰:
                  
                  ${filesSummary.join('\n')}
                  
                  변경사항:
                  ${allChanges}
                  
                  절대 규칙 위반사항만 집중해서 리뷰해주세요.`
                }],
                max_tokens: 8000,
                temperature: 0.3
              })
            });

            let reviewBody = `# 🤖 AI 코드 리뷰\n\n`;
            reviewBody += `## 📊 변경 통계\n`;
            reviewBody += `- 파일: ${filesToReview.length}개\n`;
            reviewBody += `- 추가: +${diff.data.files.reduce((sum, f) => sum + f.additions, 0)}\n`;
            reviewBody += `- 삭제: -${diff.data.files.reduce((sum, f) => sum + f.deletions, 0)}\n\n`;

            if (response.ok) {
              const result = await response.json();
              reviewBody += result.choices[0].message.content;
            } else {
              reviewBody += `⚠️ 리뷰 실패: ${response.status}`;
            }

            // PR 리뷰 제출
            await github.rest.pulls.createReview({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
              body: reviewBody,
              event: 'COMMENT'
            });

            console.log('✅ Review completed!');